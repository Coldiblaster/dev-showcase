# =============================================================================
# Dev Showcase — Variaveis de Ambiente
# =============================================================================
# Copie este arquivo para .env.local e preencha os valores.
# Nenhuma variavel e obrigatoria — recursos que dependem delas ficam
# desabilitados graciosamente quando ausentes.
# =============================================================================

# --- OpenAI (Chat IA + Code Review) -----------------------------------------
# Necessaria para o chat assistente e o revisor de codigo com IA.
# Crie em: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# --- Resend (Formulario de Contato + Newsletter) ----------------------------
# Necessaria para envio de emails pelo formulario de contato e newsletter.
# Crie em: https://resend.com/api-keys
RESEND_API_KEY=
# Email de destino para o formulario (default: email do PERSONAL em constants.ts)
RESEND_TO_EMAIL=
# Dominio verificado no Resend para envio (ex: portfolio@seudominio.com)
# Se vazio, usa onboarding@resend.dev (limitado a 100/dia)
RESEND_FROM_EMAIL=

# --- Newsletter Broadcast --------------------------------------------------
# Token secreto para disparar envio em massa (curl). Gere um valor aleatorio.
# Ex: openssl rand -hex 32
NEWSLETTER_BROADCAST_TOKEN=

# --- reCAPTCHA v3 (Protecao anti-bot no Contato) ----------------------------
# Necessarias para proteger o formulario de contato contra bots.
# Crie em: https://www.google.com/recaptcha/admin
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=

# --- DeepL (Traducao Automatica — preferencial) -----------------------------
# Usada pelos scripts de traducao (pnpm translate).
# Crie em: https://www.deepl.com/pro-api
DEEPL_API_KEY=

# --- Google Cloud Translation (Fallback de traducao) -------------------------
# Usada como fallback se DeepL nao estiver configurada.
# Crie em: https://console.cloud.google.com/apis/credentials
GOOGLE_CLOUD_API_KEY=

# --- Upstash Redis (Metricas ao Vivo) ----------------------------------------
# Necessarias para o contador de visitantes e page views.
# Crie em: https://console.upstash.com — plano Free (256MB)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# --- Giscus (Comentarios via GitHub Discussions) -----------------------------
# Necessarias para os comentarios ao final de cada conteudo.
# Configure em: https://giscus.app — escolha seu repositorio e categoria
NEXT_PUBLIC_GISCUS_REPO=
NEXT_PUBLIC_GISCUS_REPO_ID=
NEXT_PUBLIC_GISCUS_CATEGORY=General
NEXT_PUBLIC_GISCUS_CATEGORY_ID=

# --- PageSpeed Insights (Lighthouse na pagina /performance) -------------------
# Necessaria para buscar scores do Lighthouse automaticamente via API.
# Crie em: https://console.cloud.google.com/apis/credentials
# Habilite a API: PageSpeed Insights API
PAGESPEED_INSIGHTS_API_KEY=

# --- Traducao (Configuracao opcional) ----------------------------------------
# Idioma fonte para os scripts de traducao (default: pt-BR)
TRANSLATION_SOURCE=
